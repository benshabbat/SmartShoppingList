import { AddItemFormUI } from './AddItemFormUI'

/**
 * Container component that renders the AddItemForm
 * NO PROPS DRILLING - everything handled in AddItemFormUI!
 */
export const AddItemForm = () => {
  return <AddItemFormUI />
}
